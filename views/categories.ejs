<%- include('header.ejs') %>

<div class="ui container">
    <div class="row">
        <% for(var i=0;i<categories.length;i++) { %>
            <div class="col-sm-4">
                <h4><%- categories[i].name %></h4>
                <a href="/api/categoryEdit-<%- categories[i]._id %>">
                    <button type="button" class="btn-primary">Edit</button>
                </a>
                <a data-toggle="modal" data-target="#exampleModal<%- i %>">
                    <button type="button" class="btn-primary">Delete</button>
                </a>

                <%- include('deleteModal.ejs',{link: `/api/category/admin/delete/${categories[i]._id}`, Sno: i }) %>
            </div>
        <% } %>
    </div>
</div>