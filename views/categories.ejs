<%- include('header.ejs') %>

<% console.log(categories[0]._id) %>

<div class="container">
    <div class="row">
        <% for(var i=0;i<categories.length;i++) { %>
            <div class="col-sm-4">
                <h4><%- categories[i].name %></h4>
                <a href="/api/categoryEdit/<%- categories[i]._id %>">Edit</a>
            </div>
        <% } %>
    </div>
</div>